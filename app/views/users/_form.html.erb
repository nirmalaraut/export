<%= form_with(model: user) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :item %>
    <%= form.text_field :item %>
  </div>

  <div class="field">
    <%= form.label :total_sales %>
    <%= form.number_field :total_sales %>
  </div>

  <div class="field">
    <%= form.label :percentage_of_gross %>
    <%= form.text_field :percentage_of_gross %>

  </div>



  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
